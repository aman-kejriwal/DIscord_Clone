/** @type {import('next').NextConfig} */
const nextConfig = {
    // images:{
    //     domains:[
    //         "uploadthing.com"
    //     ]
    // },
    images: {
        domains: ['utfs.io', /* other domains */],
      },
};

export default nextConfig;
